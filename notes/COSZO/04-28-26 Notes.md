I have restructured the [[code migration]] today because there are several changes on the workflow, steps, and execution that was left out. 
I have ironed out the full migration plan and started making these changes in the dev/maleen
Then all of the code will be migrated in to the [coszo-hub/prest](https://github.com/coszo-hub/PREST)

## Migration status — end of 2026-04-28

| Phase | Item | Status |
|---|---|---|
| Phase 0 | `compute_variability` refactor + CSV schema | ✓ done before today (`7f961c0`) |
| Phase 0b | `bin/compare_gap_algos.py` side-by-side harness | ✓ (`88337ca`) |
| Cleanup | Remove `original_code/` archive | ✓ (`88c14fd`) |
| Phase 1 | Per-day stats CSV writer | ✓ (`546f7c8`, anomaly columns added in `4c5de86`) |
| Phase 1 | `bin/sync_metrics.sh` + cron entry | ✓ (`bb4c64b`) |
| Phase 1 | Extract `detect_gaps_legacy` / `detect_gaps_anomaly` behind `GapResult` | ✓ (`4c5de86`) — `bin/gap_algorithms.py` |
| Phase 1 | Add `gap_algo = legacy\|anomaly` to `run_prest.txt` | ✓ (`4c5de86`) — default `legacy` |
| Phase 1 | Wire pipeline to dispatch through `detect_gaps()` | ✓ (`4c5de86`) — replaces ~240 inline lines |
| Phase 1 | Anomaly columns in per-day CSV (`dt_true`, `n_ideal`, `true_missing`, `jitter_unstable`, `frac_maxabs`, `n_gaps_raw`) | ✓ (`4c5de86`) |
| Phase 1 | Remove auto-diagnostic figure call from cron | ✓ (`4c5de86`) |
| Phase 1 | `bin/plot_from_netcdf.py` refactor through `gap_algorithms` | ✓ (`721b29a`) |
| Phase 1 | `bin/compare_gap_algos.py` refactor through `gap_algorithms` | ✓ (`721b29a`) |
| Phase 1 | `testk/pull_data.py` refactor through `gap_algorithms` | ✓ (`721b29a`) — `TESTK_GAP_ALGO` env var toggle |
| Phase 1 | `--save-nc` CLI flag on pipeline; deprecate `save_netcdf` param | ✓ (`837dd0d`) |
| Phase 1 | PREST repo initial state | ⏳ committed locally at `/tmp/PREST`, push blocked on permissions (`MaleenKidiwela` lacks org write on `coszo-hub`) |
| Phase 1 | `--gap-algo` flag on `bin/plot_from_netcdf.py` for offline experiments | ✓ (`e5770da`) |
| Phase 2 | A — `jitter_unstable` action: warn + continue (CSV-only flag, no email) | ✓ already aligned in current code |
| Phase 2 | B — gap-detected email: not added; gaps remain in `output/diagnostics/gap_*.txt` + CSV | ✓ already aligned |
| Phase 2 | C — keep 50 ms / 5% sp_alert thresholds through Phase 3 | ✓ guardrail recorded in §5 |
| Phase 2 | D — short-window fallback `MIN_N_FOR_ANOMALY=100` | ✓ (`f2b1794`) |
| Phase 2 | E — deployment-boundary detection + legacy fallback + CSV flag | ✓ (`f2b1794`) — proper split-per-slice deferred |
| Phase 2 | F — William sign-off (fragmented MiniSEED + canary station/duration) | ⛔ pending external |
| Phase 3 | Canary on SLBS 2025-01-01 → 2025-01-07 | ⛔ pending VM access + William |
| Phase 4 | Remove legacy code path; drop `gap_algo` param; doc cleanups | ⛔ pending Phase 3 success |
| Logistics | PREST repo initial push (`coszo-hub/PREST`) | ⏳ committed locally at `/tmp/PREST`, needs `MaleenKidiwela` org write |

Today's 10 commits on `dev/maleen` (pushed to `mikaut/coszo-data-collection`): `163b36a..f2b1794`.

Phase 1 is complete; Phase 2 is complete to the extent code changes are required (A/B/C/D/E). What remains is purely external — William's sign-off, VM access, and the org-permission grant for PREST.

Phase 1 is functionally complete — algorithm is swappable via `gap_algo` in `param/run_prest.txt`. End-to-end smoke test on saved 2015-01-30 NetCDFs (all 3 stations) shows both algorithms agree (1 gap, 2 segments each, sub-ppm rate difference). Ready for Phase 2 decisions and Phase 3 canary once William signs off and VM access is restored.
