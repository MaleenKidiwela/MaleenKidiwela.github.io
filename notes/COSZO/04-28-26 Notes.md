I have restructured the [[code migration]] today because there are several changes on the workflow, steps, and execution that was left out. 
I have ironed out the full migration plan and started making these changes in the dev/maleen
Then all of the code will be migrated in to the [coszo-hub/prest](https://github.com/coszo-hub/PREST)

## Migration status — end of 2026-04-28

| Phase | Item | Status |
|---|---|---|
| Phase 0 | `compute_variability` refactor + CSV schema | ✓ done before today (`7f961c0`) |
| Phase 0b | `bin/compare_gap_algos.py` side-by-side harness | ✓ today (`88337ca`) |
| Cleanup | Remove `original_code/` archive | ✓ today (`88c14fd`) |
| Phase 1 | Per-day stats CSV writer (legacy fields) | ✓ today (`546f7c8`) |
| Phase 1 | `bin/sync_metrics.sh` + cron entry | ✓ today (`bb4c64b`) |
| Phase 1 | PREST repo initial state | ⏳ committed locally at `/tmp/PREST`, push blocked on permissions |
| **Phase 1** | **Extract `detect_gaps_legacy` / `detect_gaps_anomaly` behind `GapResult` interface** | ⛔ **not started — this is the core algorithm swap** |
| Phase 1 | Add `gap_algo = legacy\|anomaly` to `run_prest.txt` | ⛔ not started |
| Phase 1 | Wire pipeline to call `detect_gaps_*` instead of inline legacy block | ⛔ not started |
| Phase 1 | Add anomaly columns to per-day CSV (`dt_true`, `true_missing`, `n_ideal`, `jitter_unstable`, `frac_maxabs`) | ⛔ not started |
| Phase 1 | `testk/pull_data.py` refactor to use shared interface | ⛔ not started |
| Phase 1 | Remove auto-diagnostic figure call from cron | ⛔ not started |
| Phase 1 | `--save-nc` CLI flag on pipeline; deprecate `save_netcdf` param | ⛔ not started |
| Phase 1 | `plot_from_netcdf.py` refactor to use shared interface | ⛔ not started |
| Phase 2 | Open decisions: `jitter_unstable` action, gap-detected email, deployment-boundary policy, short-window minimum, William sign-off | ⛔ not started |
| Phase 3 | Canary cutover on AXBS (1 week), then SLBS, then SUM1 | ⛔ not started |
| Phase 4 | Remove legacy code path; drop `gap_algo` param; doc cleanups | ⛔ not started |

Today's commits on `dev/maleen` (not pushed to remote yet): `88c14fd → 88337ca → 546f7c8 → bb4c64b`.
