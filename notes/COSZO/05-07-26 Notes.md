## Website restructure — first pass through Data rename

Acted on the website action items from [[04-23-26 Notes]] (meeting with William and Mika). Worked entirely in `~/Documents/coszo-hub.github.io/build_pages.py` — that script is the single source of truth for the site; it regenerates every HTML file on each run.

### Nav restructure (in `build_header`)

Old: `Home (with dropdown) | Infrastructure | Data Products | People | Outreach | Early Warning`

New: `Home | Science | Infrastructure | Data | People | Outreach | Early Warning`

- **Home** — dropdown removed. Plain link to `index.html`.
- **Science** — new top-level. Not clickable (rendered as a `<span>` with `aria-haspopup`); only the dropdown items navigate. Dropdown:
  - Motivation → `science.html#motivation`
  - Objectives → `science.html#objectives`
  - Publications → `publications.html`
- **Infrastructure** — added **Sites** as the second item, right after Overview:
  - Overview · **Sites** · Existing Instruments · COSZO Instruments · Future Opportunities
- **Data** — renamed from "Data Products". Filename also renamed `data-products.html → data.html`. Kept the existing wide Pressure / Motion / Water product list in the dropdown (per discussion).
- People / Outreach / Early Warning unchanged for this round.

Active-key system in `build_header` gained a new `science` key. `motivation.html` and `scientific-objectives.html` had their active key flipped from `home` to `science`.

### New pages

All added to `PAGES` and built successfully. Reused existing copy where it already existed in the build script.

- **`science.html`** — combined page with two anchored scrollable sections (`#motivation`, `#objectives`). Reuses the original `MOTIVATION_BODY` and `SCIOBJ_BODY` content. Sidebar links to both anchors plus Publications. Standalone `motivation.html` and `scientific-objectives.html` are still built (unlinked from nav but kept so any external/inbound links don't 404).
- **`publications.html`** — stub. Hero + "under construction ahead of May SAC meeting" lede + invite to send publications.
- **`sites.html`** — overview with three site cards (Hydrate Ridge, Slope Base, Axial Seamount), each routing to its own page. Card layout uses the existing `hub-cards cols-3` pattern. Trailing note that more sites will be added.
- **`hydrate-ridge.html`**, **`slope-base.html`**, **`axial-seamount.html`** — per-site stubs with hero + breadcrumb (Home / Infrastructure / Sites / <site>) + placeholder for instrument list, depth, location, science role.

### Data rename

- Global replace `data-products.html → data.html` in `build_pages.py` (23 occurrences: nav, hero CTA, product cards list, ASP page back-link, footer, PAGES entry).
- PAGES title updated `"Data Products · COSZO"` → `"Data · COSZO"`.
- Old `data-products.html` file removed from disk after the rebuild (build no longer writes it).

### Build verification

`python3 build_pages.py` regenerated **24 pages** including the five new ones (`science`, `publications`, `sites`, `hydrate-ridge`, `slope-base`, `axial-seamount`) and `data.html`. No remaining references to `data-products.html` anywhere in the rendered output.

### Not done in this pass (next time)

- Body content for `data.html` is still the old "Data Products" hero + product cards. The 04-23 plan calls for it to become a landing page about Earthscope storage, GitHub scripts for derived products, metadata/instrument logs, and data availability tracking. Cards can stay as a sub-section but the framing needs to change.
- Per-site stubs need real content (instrument list, depth, location, schematic).
- Publications list needs actual entries.
- People page still needs Installation vs Operation split + photos.
- Visual editor wiring for GitHub Pages CMS — `.pages.yml` already exists but new pages aren't registered there yet.
- Early Warning section: meeting flagged it for possible removal/restructure, deferred.
