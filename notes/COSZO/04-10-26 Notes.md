## Session Summary — April 10, 2026

**Focus:** Revising data quality diagnostic figures in `diagnose_timing.py` — ensuring figures are well-understood, correctly labelled, and scientifically meaningful.

---

## Carry-over from April 9

- `diagnose_timing.py` has three modes: `single`, `collect`, `plot`
- One day test run completed successfully (RS01SLBS, 2025-01-01)
- 31-day collect run kicked off on old computer (RS01SLBS, 2025-01-01 to 2025-01-31)
- Jitter metric currently unitless (0–0.5) — needs to be converted to **ms**

## Open revision items

- Jitter metric should be in ms, not unitless
- Need to revisit what each figure actually shows and whether it communicates the right information
- Figure descriptions and axis labels need to be clear and scientifically meaningful

## Action items — diagnose_timing.py

### Per-day figure — revised 4-panel layout

| Panel | Content | X-axis | Y-axis |
|-------|---------|--------|--------|
| 1 | Histogram of residuals | Residual (ms), relative to best-fit line | Count |
| 2 | Histogram of Δt relative to nominal | (Δt − sp_nominal) × 1000 in ms | Count |
| 3 | Residuals scatter over time | Sample index | Residual (ms), colour by segment |
| 4 | Δt scatter with gap threshold | Sample index | Δt (s) |

- [ ] Histogram y-axis: switch from density to raw sample count (Panel 1)
- [ ] Add Panel 2: histogram of `(Δt − sp_nominal) * 1000` in ms with vertical line at 0
- [ ] Renumber existing Panel 2 → Panel 3, Panel 3 → Panel 4
- [ ] Jitter metric: convert from unitless (0–0.5) to ms
- [ ] Add `sp_fit` (least-squares slope through all timestamps) to `compute_stats()`, store in metrics CSV, add as a reference line to Panel 4, and plot alongside `sp_calc` in summary Figure 1
- [ ] For each gap, add to `timing_stats.txt`: gap duration in seconds and estimated missing samples (e.g. `gap 1: 45.2 s (~45 missing samples)`)
- [ ] Add to metrics CSV: `gap_max_missing` (largest single gap in samples), `gap_total_missing` (total missing samples across all gaps), `gap_total_duration_s` (total time missing in seconds)
- [ ] Add binary columns `missing_start` and `missing_end` to metrics CSV — True if data is absent at the beginning or end of the day (threshold: first/last sample more than 1 × sp_nominal from midnight)
- [ ] Add `data_start` and `data_end` (UTC timestamp strings of first and last sample) to metrics CSV
- [ ] Use `data_start` and `data_end` to compute `expected_npts` — based on actual data window (`round((data_end - data_start) / sp_calc) + 1`) rather than assuming a full 86400s day. This separates missing samples within the data from missing data at day boundaries (captured by `missing_start`/`missing_end`)
- [ ] Sample count plot: green = all expected samples present (n_points == expected_npts exactly or within 1 sample), red = any samples missing, grey = no data
- [ ] All summary figure x-axis date labels: auto-scale based on date range — show individual dates (e.g. `Jan 01`) for short ranges, year-month for longer ranges

---
# Meeting Summary: Mika and Maleen - April 10, 2026

**Main Topics Discussed:**

**1. System Setup and Repository Management**

- Maleen Wijeratna successfully installed and cloned the repository on both personal computer and virtual machine
    
- The old repository was renamed to "old" but still takes up space
    
- Mika deleted old mini seed files to free up storage (currently 4GB used, 15GB free)
    

**2. Data Processing Constraints**

- The virtual machine has only one core, limiting parallel processing
- Processing takes approximately 1 minute per day of data (30 minutes for one month)
- Historical and real-time processing cannot run simultaneously
- Complete historical processing will take approximately one week

**3. Data Quality Analysis**

Maleen Wijeratna conducted detailed quality assessments on January 2025 data, finding:

- **Timing jitter:** Most data within 5-15 milliseconds, varying by day and instrument
    
- **Sample rate variations:** Generally stable around nominal rates, with XAB at 0.067 Hz (15-second sampling)
    
- **Data gaps:** Multiple days show gaps (up to 37 gaps in a single day)
    
- **Incomplete days:** Some days missing data at beginning or end
    

**4. Next Steps**

- Maleen Wijeratna will run data quality assessment across entire historical dataset locally (without creating mini-seed files initially)
    
- Will generate metrics including jitter, gap counts, and sample rate calculations
    
- Need to verify mini-seed file format maintains data integrity
    
- Meeting invites were clarified; Maleen Wijeratna added to monthly data streams meeting (every 4 weeks on the 22nd)


Yes, there was discussion about VM upgrade. Toward the end of the meeting, Maleen Wijeratna mentioned that processing all the data would benefit from several cores (at least 3) with more memory. Mika responded positively, noting that:

- Setting up additional virtual machines is not a big deal and can be done in less than an hour by Ken
- The current VM setup (20GB storage, 2GB memory) is just standard, but can be modified
- Multiple machines were always expected to be part of the plan
- The cost is minimal (just Ken's time for setup)

**Next Steps:**

Mika suggested that Maleen Wijeratna should:

1. Bring up the VM upgrade requirements with William at their next meeting (scheduled for next week)
2. Work out the specific needs with Ken, who set up the initial machine access
3. Discuss what specifications would be needed for improved parallel processing

The upgrade would help reduce the historical data processing time from a full week to just a few days, making it easier to catch issues early rather than after a week of processing.